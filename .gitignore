# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-server
*.local
*.tsbuildinfo
coverage
*.tgz

# Runtime data dir: SQLite DB, JSON cache and the generated session key.
# Nothing in here belongs in git — only the directory itself.
cache/*
!cache/.gitkeep
cache-fresh/
*.db
*.db-shm
*.db-wal
.env
.env.local

# Real league config — contains access codes; mounted as a volume in prod
config/leagues.json

# Claude Code internal
.claude/

# Pre-publication audit notes — not part of the project
audit/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
Thumbs.db
desktop.ini
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
